server {
    listen 80;
    server_name initcrm.ru;

    location /front {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /front/index.html;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
